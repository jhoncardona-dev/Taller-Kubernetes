services:
  web:
    build: .
    ports: ["8090:5000"]
    environment: [DB_HOST=db]
    depends_on: [db]

  db:
    image: postgres:16
    environment: [POSTGRES_PASSWORD=4628]
    volumes: [datos:/var/lib/postgresql/data]

volumes:
  datos:
        

    
